<script setup>
import { %%TABLE_IMPORTS%% } from 'invue/tables'
import { useInvueTable } from 'invue/tables'
import { PanelLayout } from 'invue/panels'
import { Link } from '@inertiajs/vue3'

const table = useInvueTable('%%TABLE_PROP%%')
</script>

<template>
    <PanelLayout>
        <div class="mb-6 flex items-center justify-between">
            <h1 class="text-xl font-semibold text-gray-900">%%NAVIGATION_LABEL%%</h1>
            <Link
                href="%%CREATE_URL%%"
                class="rounded-md bg-green-600 px-3 py-1.5 text-sm font-semibold text-white hover:bg-green-500"
            >
                New %%MODEL_LABEL%%
            </Link>
        </div>

        <Table :table="table" searchable>
%%TABLE_COLUMNS%%
            <ActionsColumn
                label="Actions"
                align="end"
                trigger="inline"
                :actions="(row) => [
                    { label: 'Edit', icon: 'pencil', url: `%%EDIT_URL_BASE%%/${row.%%PRIMARY_KEY%%}/edit` },
                    {
                        label: 'Delete',
                        icon: 'trash',
                        color: 'red',
                        url: `%%EDIT_URL_BASE%%/${row.%%PRIMARY_KEY%%}`,
                        method: 'delete',
                        requiresConfirmation: true,
                        confirmationTitle: 'Delete %%MODEL_LABEL%%?',
                        confirmationText: 'This action cannot be undone.',
                        confirmationButtonLabel: 'Delete',
                    },
                ]"
            />
        </Table>
    </PanelLayout>
</template>
